# 第四问实施与评估计划

用户授权：采用方案B完成第四问，补充前三问模型文档，单独评估方案C；前三问的方法与结果受保护。评估报告直接放项目根目录。

实施范围：新增 `run_q4.py`、`q4/`、`tests/test_q4.py`、`output/q4-2/`、`output/q4-3/`、`output/q4_experiments/`；仅在原数学模型文档末尾追加第四问。原 `microgrid/`、`run.py`、附件、前三问结果及测试不改。

方案B：未来价格未知、按供电段实际价格结算；0点价格由历史同刻预测，第三问制度在6/12/18点使用此前一小时已实现价格作限幅衰减修正。净负荷预测复用原ForecastEngine，自适应预测和分位裕度。Q4-2不引入附件3。按原计费口径，以最终量相对0点计划计偏差。

一月选参：价格预测候选为昨日同刻、七日加权；净负荷分位数候选为0.50/0.65/0.80。每个制度6个候选，全部记录，不根据2—12月成绩重新选择。首日两制度统一零正常购电冷启动；状态从6000 kWh连续传递。12月31日跨年部分使用同原点预测，仅用于前瞻。

方案C评估：在各制度B已选价格预测器基础上，固定56日误差窗口、半衰期28日、最多5个成对历史场景、CVaR置信水平0.9，比较净负荷场景+点价格(C_fixed)、价格净负荷联合场景(C0)、联合场景+总费用CVaR权重0.15(C15)。C0/C15以一月成本选出C推荐版本，二月以后不调参。B始终作为正式交付方案。

主指标：2—12月真实总费用（元，越低越好）。同时报告正常/调整/紧急费、最坏日、日费用95%分位、紧急量、月度差、期末电量、运行时间。比较自然热身和固定B二月初值，隔离初始资产差异。按7日块配对bootstrap(种子20260913，1000次)估计平均日费用差不确定性，不能据此推断其他年份必然节费。

正确性门槛：实际供需和储能递推残差≤1e-6 kWh、充放电与容量不越界、不同时充放电、跨日连续、全年365×144、输出334天、实际价格独立重算费用、模板与轨迹匹配；未知未来扰动不得改变此前计划。旧测试应继续通过，保护文件按SHA-256核验。

执行顺序：独立副本实现 → 单元/短期运行 → 一月选参 → 全年B与C对照 → 独立审计 → 文档追加与评估报告 → 仅将新文件及授权追加文档同步原项目。最多运行既定候选及必要故障修复，不依据留出结果扩大搜索。

补充解释性检查：B参数冻结后，为回答第四问承接的第三问“是否引入后续预报”要求，增加Q4-3仅0点使用光伏预报、全天不调整正常购电的对照。该对照固定为B所选价格预测和分位数，并固定2月初储电量为B值；不调参、不参与主模型选择，仅解释整套日内预报与调整机制的联合价值。没有将其视为单独价格更新收益。
