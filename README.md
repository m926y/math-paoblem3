# C题第一至三问：可复现代码与结果

本目录统一实现第一、二、三问的输入读取、模型运行、官方模板输出和验证。默认附件目录为本目录下的 `附件/`；所有入口均可用 `--attachment-dir` 指定另一份同结构数据。第四问按当前项目范围排除。

## 环境与运行

```powershell
python -m pip install -r requirements.txt
python run_q1.py
python run_q2.py
python q3_model.py
python validate_project.py
```

默认输出分别位于 `output/q1/`、`output/q2/`、`output/q3/`。正式工程基线和模型对照保存在 `output/engineering_baseline/` 与 `output/model_versions/`。`validate_project.py --rerun` 会重新执行全部第一至三问及已定义的对照实验，随后验证输入、约束、模板与费用回归；普通运行只验证已有结果，耗时更短。

依赖为 NumPy、SciPy、Matplotlib 和 openpyxl。结果工作簿直接加载附件5中的官方 `result1.xlsx`、`result2.xlsx`、`result3.xlsx` 后填值，保留其工作表结构和格式。

## 文件职责

| 文件 | 内容 |
|---|---|
| `project_io.py` | 统一路径、SHA256、严格输入校验、result1/result2官方模板写出 |
| `dispatch.py` | 第一、二问单日线性规划与实际储能闭环 |
| `forecast.py` | 七日及扩展历史预测、单侧安全裕度 |
| `run_q1.py` | 第一问入口 |
| `q2_core.py` | 第二问工程基线和严格因果1月初始化的共享实现 |
| `q2_output.py` | 第二问工作簿、CSV与图形输出 |
| `run_q2.py` | 第二问同星期预测优化主入口，默认零计划冷启动和0.90净负荷安全分位数 |
| `run_q2_legacy.py` | 保持旧数学逻辑的工程回归基线入口 |
| `run_q2_causal.py` | 第二问严格因果模型入口，支持两种冷启动 |
| `q3_forecast.py` | 第三问光伏预报、历史误差安全裕度和负荷实时修正 |
| `q3_dispatch.py` | 第三问24小时MPC与十分钟实际执行 |
| `q3_xlsx_io.py` | 第三问输入接口和result3官方模板写出 |
| `q3_model.py` | 第三问全年滚动仿真、费用与模型参数入口 |
| `validate_project.py` | 第一至三问统一回归、失败测试、模板和约束验证 |

## 统一数据和校验

`project_io.py` 要求：附件1为144个十分钟点；附件2为2025年连续365天、每天144点；附件3完整覆盖365天与0/6/12/18点四个发布时间、每条24小时预测。日期重复或缺失、发布时间重复或缺失、空白、非数值、NaN、无穷值和负值均直接报错，不再转换为零。

当前统一验证包含148项检查，全部通过。检查范围包括四类错误输入失败测试、结果工作表名称、334天日期、逐日及全期电量和费用、SOC范围、能量平衡残差、充放电重叠、极端缺电和光伏过剩压力测试，以及三问工程基线费用回归。

## 问题1

问题1采用确定性单日线性规划。每个十分钟时段满足：

`g_t + pv_t + r_t = load_t + c_t + spill_t`

`E_t = E_(t-1) + 0.9*c_t - r_t/0.9`

初始与日终储电量均为6000 kWh，SOC范围为1200—10800 kWh，充放电功率上限为5000 kW，即每个十分钟最多833.3333 kWh。当前结果为：

| 指标 | 数值 |
|---|---:|
| 正常购电量 | 59,482.6990 kWh |
| 正常购电费用 | 35,126.9486 元 |
| 充电量 | 20,740.6661 kWh |
| 放电量 | 16,799.9396 kWh |
| 最大能量平衡残差 | 2.27e-13 kWh |

## 问题2

问题2在0点用历史数据形成次日计划；正常购电计划固定，实际运行时储能基于当前十分钟实测供需闭环调整，剩余缺口按当时电价的5倍紧急购电，剩余过剩电量弃置。2月1日至12月31日共334天的费用口径为：

`C2 = sum(p_t*g_t) + 5*sum(p_t*emergency_t)`

### 工程回归基线

`run_q2_legacy.py` 保留修复前的1月初始化数学逻辑，用于证明路径、校验和模板修改没有主动改变既有数值。它在1月使用当天完整实际负荷和光伏，不能作为严格因果策略的最终版本。

| 指标 | 工程基线 |
|---|---:|
| 2月初始SOC | 5,869.3480 kWh |
| 正常购电费用 | 16,291,622.9931 元 |
| 紧急购电量 | 405,289.3713 kWh |
| 紧急购电费用 | 2,407,040.8301 元 |
| 总费用 | **18,698,663.8232 元** |

旧README中的16,874,623.2861元与当前代码和结果不一致，已废止。

### 严格因果1月初始化

`run_q2_causal.py` 保留原最近7天预测，用于显式运行两种冷启动对照。两种冷启动都完整保留：

```powershell
python run_q2_causal.py --cold-start zero
python run_q2_causal.py --cold-start baseline
```

- `zero`：1月1日没有历史信息时令正常购电计划为零，用当前实测供需和储能执行，剩余缺口紧急购电。这是信息边界最严格的主版本。
- `baseline`：仅在1月1日使用附件1中的一天负荷和光伏曲线作为事先给定的代表性基准，之后改用当时已经积累的历史数据。这是冷启动敏感性对照。

两种冷启动的一月运行费用分别为2,634,657.8281元和2,489,874.2811元，相差144,783.5470元；到2月1日时二者都收敛到10,450.3523 kWh，2—12月结果完全相同：

| 指标 | 因果零计划 | 因果基准曲线 |
|---|---:|---:|
| 正常购电费用 | 16,289,477.6578 | 16,289,477.6578 |
| 紧急购电费用 | 2,407,040.8301 | 2,407,040.8301 |
| 紧急购电量 | 405,289.3713 | 405,289.3713 |
| 总费用 | **18,696,518.4879** | **18,696,518.4879** |

因此冷启动选择影响1月热身过程，但在当前数据和模型下不影响题目要求的2—12月答案。与工程基线相比，严格因果版本总费用低2,145.3354元；该差异来自初始化模型变化，应作为独立模型版本报告。

### 同星期预测优化主版本

`run_q2.py` 直接运行优化后的现有主入口，不再新建另一套入口。它使用目标日前7、14、21、28天的同星期曲线，权重为0.50、0.25、0.15、0.10，并对历史净负荷低估误差取0.90分位数。预测和误差样本都严格截止到目标日前。

| 指标 | 优化主版本 |
|---|---:|
| 2月初始SOC | 3,334.9808 kWh |
| 正常购电费用 | 13,855,599.0460 元 |
| 紧急购电量 | 123,296.7784 kWh |
| 紧急购电费用 | 703,172.5686 元 |
| 总费用 | **14,558,771.6146 元** |

相对原严格因果最近7天预测，优化主版本减少总费用4,137,746.8733元，即22.13%，并减少紧急购电281,992.5929 kWh。收益主要来自识别工作日与周末的星期周期。

安全裕度敏感性实验如下。`net`直接对净负荷低估误差取分位数，`separate`分别对负荷低估和光伏高估取分位数。

| 安全模式 | 分位数 | 正常购电费用 | 紧急购电费用 | 紧急购电量 | 弃电量 | 总费用 |
|---|---:|---:|---:|---:|---:|---:|
| net | 0.80 | 13,012,574.1428 | 1,608,928.8545 | 277,413.7540 | 2,347,121.4663 | 14,621,502.9974 |
| net | 0.85 | 13,351,438.7518 | 1,137,893.0728 | 197,548.0631 | 2,737,444.5841 | **14,489,331.8246** |
| net | 0.90 | 13,855,599.0460 | 703,172.5686 | 123,296.7784 | 3,362,443.5276 | 14,558,771.6146 |
| separate | 0.80 | 13,199,342.8703 | 1,313,811.6540 | 229,820.1593 | 2,568,533.9654 | 14,513,154.5242 |
| separate | 0.85 | 13,582,871.8249 | 934,968.5156 | 164,676.9086 | 3,043,960.5129 | 14,517,840.3405 |
| separate | 0.90 | 14,147,765.5385 | 597,328.2857 | 105,627.7244 | 3,770,833.8710 | 14,745,093.8243 |

全年描述性最低值是`net/0.85`。当前主入口仍默认`net/0.90`，因为它在预先划分的2—6月选择期费用最低；7—12月作为留出期，不用于挑选参数。提高分位数会减少紧急购电，但增加正常购电和弃电，因此不能仅以紧急购电量选择参数。

## 问题3与对照实验

第三问模型、费用口径和更新时刻实验见 `README_Q3.md`。模型对照的机器可读汇总为 `output/model_versions/model_comparison.csv`、`.json` 和 `.xlsx`。

## 结果使用规则

工程基线用于回归，不代表所有建模假设均已验收。第二问论文主结果采用 `run_q2.py` 的严格因果同星期预测版本，同时保留最近7天预测和两种冷启动作为对照。第三问当前主版本采用0/6/12/18点累计更新、终端价值系数0。论文源文件未包含在项目中，因此本次只更新代码、README和结果表；将数值写入论文时应直接引用各版本 `summary.json` 或模型对照表，避免手工抄录旧值。
